apiVersion: v1
kind: Service
metadata:
  name: test-db
spec:
  type: NodePort
  selector:
        app: test-db
  ports:
    - port: 5432           # service port
      targetPort: 5432   # container port
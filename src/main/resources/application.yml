spring:
  application:
    name: k8s-demo
  datasource:
    url: jdbc:postgresql://test-db:5432/testdb  # ← use service name
    username: postgres
    password: Keat6951
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    servers:
      - url: https://www.sikeat.me
        description: Production

server:
  port: 8080